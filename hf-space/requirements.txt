# RAGmind HF Space - Python deps

# Web
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-multipart>=0.0.9

# Auth (crypto extra needed for ES256 JWKS verification)
PyJWT[crypto]>=2.8.0

# Supabase (pgvector store + auth + storage)
supabase>=2.5.0

# Document loading + chunking
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
langchain-core>=0.2.0
pypdf>=4.0.0

# Embeddings + reranking
sentence-transformers>=3.0.0

# LLM: generation runs on Groq (OpenAI-compatible) via httpx — no local model.
# (httpx comes in transitively via supabase.)

# Model downloads for sentence-transformers
huggingface_hub>=0.23.0
